import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d81b60",
    },
    secondary: {
      main: "#757575",
    },
  },
});

export default theme;
